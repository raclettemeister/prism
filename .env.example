# ============================================================
# PRISM Environment Variables
# Copy this file to .env and fill in your values.
# ============================================================

# ── Required ─────────────────────────────────────────────────
# Your Anthropic API key. All Claude calls use this.
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Email delivery (optional) ─────────────────────────────────
# Resend API key for email delivery.
# Without this, briefings are saved to briefings/ but not emailed.
# Get yours at: https://resend.com/
RESEND_API_KEY=re_your-resend-key-here

# ── MylifeOS integration (optional) ──────────────────────────
# Your personal Obsidian vault repo on GitHub.
# Format: "username/repo-name" (NOT a full URL)
# Used to fetch: news-interests.md, prism-feedback.md, life-context
# Without this, PRISM uses the local data/ files instead.
MYLIFEOS_REPO=yourusername/your-vault-repo

# GitHub Personal Access Token with "repo" read scope.
# Create one at: https://github.com/settings/tokens
# Required if MYLIFEOS_REPO is set and the repo is private.
MYLIFEOS_PAT=github_pat_your-token-here

# ── PRISM Portal — Live hosted briefing (v4.0, optional) ──────
# Base URL of your Cloudflare Pages deployment.
# When set: email includes "Read live →" link, HTML page is generated.
# Example: https://prism.julien.care
PRISM_PORTAL_URL=https://prism.yourdomain.com

# ── Feedback Worker — Cloudflare Worker (v4.0, optional) ──────
# Endpoint of the PRISM Feedback Worker (worker/feedback.js).
# When set: the HTML page submits feedback directly to this URL.
# Without this: the page downloads feedback as JSON for manual import.
# Example: https://prism-feedback.your-subdomain.workers.dev
FEEDBACK_WORKER_URL=https://prism-feedback.your-subdomain.workers.dev

# Shared secret between the portal page and the Cloudflare Worker.
# Set the same value in: (1) here, (2) Cloudflare Worker secrets via wrangler.
# wrangler secret put PRISM_FEEDBACK_SECRET
PRISM_FEEDBACK_SECRET=your-random-secret-here

# ── Cloudflare Worker secrets (set via wrangler, not here) ────
# These are set on the Worker directly, not in this .env file:
#   PRISM_GITHUB_TOKEN   — GitHub PAT with repo write scope
#   PRISM_GITHUB_REPO    — e.g. "raclettemeister/prism"
#   PRISM_FEEDBACK_SECRET — same value as above
#   ALLOWED_ORIGIN       — e.g. "https://prism.julien.care"
