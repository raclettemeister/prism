# ============================================================
# PRISM Environment Variables
# Copy this file to .env and fill in your values.
# ============================================================

# ── Required ─────────────────────────────────────────────────
# Your Anthropic API key. All Claude calls (scoring, analysis,
# synthesis, validation) use this.
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Email delivery (optional) ─────────────────────────────────
# Resend API key for email delivery.
# Without this, briefings are saved to briefings/ but not emailed.
# Get yours at: https://resend.com/
# You also need a verified sending domain configured in Resend.
RESEND_API_KEY=re_your-resend-key-here

# ── MylifeOS integration (optional) ──────────────────────────
# Your personal Obsidian vault repo on GitHub.
# Format: "username/repo-name" (NOT a full URL)
# Used to fetch: news-interests.md, prism-feedback.md, life-context
# Without this, PRISM uses the local data/ files instead.
MYLIFEOS_REPO=yourusername/your-vault-repo

# GitHub Personal Access Token with "repo" read scope.
# Create one at: https://github.com/settings/tokens
# Required if MYLIFEOS_REPO is set and the repo is private.
MYLIFEOS_PAT=github_pat_your-token-here
